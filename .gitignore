
# LEMBRETE - COMANDO PARA TROCAR SENHA ADM - RODAR NO DEVTOOLS > async function h(s){const b=new TextEncoder().encode(s);const d=await crypto.subtle.digest('SHA-256',b);return [...new Uint8Array(d)].map(x=>x.toString(16).padStart(2,'0')).join('')}
# h('NOVA_SENHA').then(console.log)